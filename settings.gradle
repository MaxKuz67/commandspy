pluginManagement {
    repositories {
        // чтобы найти все обычные плагины
        gradlePluginPortal()
        // и чтобы найти плагин userdev от NeoForge
        maven {
            name = 'NeoForged Plugin Releases'
            url  = 'https://maven.neoforged.net/releases'
        }
    }
}

plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.9.0'
}
